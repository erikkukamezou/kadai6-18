<h1>投稿一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>
<% @blogs.each do |blog| %>
  <tr>
    <td><%= picture.title %></td>
    <td><%= picture.content %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくブログを投稿する', new_picture_path %>
